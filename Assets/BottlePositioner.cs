using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BottlePositioner : MonoBehaviour {
    /*
    public GameObject bottle;

    private void OnTriggerEnter(Collider other)
    {
        Trash trash = bottle.GetComponent<Trash>();
        if(trash.attached)
        {
            trash.detach();
        }
    }
    */
}
